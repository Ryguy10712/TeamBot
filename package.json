{
  "dependencies": {
    "@types/node-cron": "^3.0.6",
    "discord.js": "^14.6.0",
    "dotenv": "^16.0.3",
    "fs": "^0.0.1-security",
    "node-cron": "^3.0.2",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "start": "node ./dist/Bot.js",
    "build": "tsc -p ./tsconfig.json",
    "build and start": "tsc -p ./tsconfig.json && node ./dist/Bot.js"
  },
  "engines": {
    "node": "18.x"
  }
}
