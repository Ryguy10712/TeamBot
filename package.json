{
  "dependencies": {
    "@prisma/client": "^4.8.0",
    "@types/node-cron": "^3.0.6",
    "discord.js": "^14.6.0",
    "dotenv": "^16.0.3",
    "node-cron": "^3.0.2",
    "prisma": "^4.8.0",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "start": "node ./dist/Bot.js",
    "build": "tsc -p ./tsconfig.json",
    "watch": "tsc -p ./tsconfig.json --watch",
    "build_and_start": "tsc -p ./tsconfig.json && node ./dist/Bot.js"
  },
  "engines": {
    "node": "18.x"
  }
}
